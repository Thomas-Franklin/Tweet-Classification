{{#partial "content"}}

<div class="row">
    <div class="col-md-8 mb-1">
        <div class="visualisation-boards">
            <ul class="nav nav-tabs mr-auto d-none" role="tablist">
                <li class="nav-item">
                    <a class="nav-link" role="tab" data-toggle="tab"
                       href="#default">Default Tab</a>
                </li>
                {{#each userResultsList}}
                <li class="nav-item">
                    <a class="nav-link" role="tab" data-toggle="tab"
                       href="#{{this.username}}">{{this.username}}</a>
                </li>
                {{/each}}
            </ul>
            <div class="tab-content">
                <div id="default" class="container tab-pane active"><br>
                    <h5>Top Users</h5>
                    <p>The users classifications will be displayed using various techniques, such as word clouds,
                        charts etc.</p>
                    <p>In the table on the right, please select the one you wish to view.</p>
                </div>
                {{#each userResultsList}}
                <div id="{{this.username}}" class="container tab-pane fade" data-rumour="{{this.countOfRumours}}" data-non-rumour="{{this.countOfNonRumours}}"><br>
                    <div class="tabs-x align-center tabs-above tab-bordered">
                        <ul class="nav nav-tabs mr-auto" role="tablist">
                            <li class="nav-item">
                                <a class="nav-link active show" id="{{this.username}}WordCloudTab" role="tab" data-toggle="tab" href="#{{this.username}}WordCloud" aria-controls="{{this.username}}WordCloud" aria-expanded="true">Word Cloud</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" id="{{this.username}}PieChartTab" role="tab" data-toggle="tab" href="#{{this.username}}PieChart" data-target="#{{this.username}}PieChart">Pie Chart</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" id="{{this.username}}BarChartTab" role="tab" data-toggle="tab" href="#{{this.username}}BarChart" data-target="#{{this.username}}BarChart">Bar Chart</a>
                            </li>
                        </ul>
                        <div class="tab-content">
                            <div id="{{this.username}}WordCloud" class="container tab-pane fade show active" aria-labelledby="{{this.username}}WordCloud"><br>
                                <img width="100%" src='data:image/png;base64,{{this.base64WordCloudImage}}'/>
                            </div>
                            <div id="{{this.username}}PieChart" class="container tab-pane fade" aria-labelledby="{{this.username}}PieChartTab"><br>
                                <div class="chart-container">
                                    <canvas id="{{this.username}}PieChartCanvas"></canvas>
                                </div>
                            </div>
                            <div id="{{this.username}}BarChart" class="container tab-pane fade" aria-labelledby="{{this.username}}BarChartTab"><br>
                                <div class="chart-container">
                                    <canvas id="{{this.username}}BarChartCanvas"></canvas>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="users-table">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title text-center">Users</h5>
                    <hr>
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th scope="col">Rank</th>
                            <th scope="col">Username</th>
                            <th scope="col"></th>
                        </tr>
                        </thead>
                        <tbody>
                        {{#each userResultsList}}
                        <tr>
                            <th scope="row">{{increment @index}}</th>
                            <td>
                                <div class="radiotext">
                                    <label for="{{this.username}}RadioInput">{{this.username}}</label>
                                </div>
                            </td>
                            <td>
                                <div class="radio">
                                    <label>
                                        <input type="radio" id="{{this.username}}RadioInput"
                                               name="userRadioGroup"
                                               data-target="#{{this.username}}">
                                    </label>
                                </div>
                            </td>
                        </tr>
                        {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-12 mb-1 mt-1">
        <div class="raw-row-data">
            <ul class="nav nav-tabs mr-auto d-none" role="tablist">
                <li class="nav-item">
                    <a class="nav-link" role="tab" data-toggle="tab" href="#none">Default Tab</a>
                </li>
                {{#each userResultsList}}
                <li class="nav-item">
                    <a class="nav-link" role="tab" data-toggle="tab"
                       href="#{{this.username}}RawData">{{this.username}} Raw Data</a>
                </li>
                {{/each}}
            </ul>
            <div class="tab-content">
                <div id="none" class="container tab-pane active"><br>
                </div>

                {{#each userResultsList}}
                <div id="{{this.username}}RawData" class="container tab-pane fade"
                     data-size="{{this.totalCountOfClassifications}}"><br>
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title text-center">Raw Data for {{this.username}}</h5>
                            <hr>
                            <div id="{{this.username}}Table"></div>
                            <div class="justify-content-center">
                                <nav>
                                    <ul class="pagination justify-content-center" id="{{this.username}}Pagination"></ul>
                                </nav>
                            </div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>

    </div>
</div>

<script src="/assets/js/users.js" type="text/javascript"></script>

{{/partial}}
{{> master path="users"}}