group 'twitter.classification.stream'

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceCompatibility = 1.8

apply plugin: 'java'
apply plugin: 'war'

dependencies {

    compile 'com.rabbitmq:amqp-client:5.1.2'

    providedCompile 'javax.servlet:javax.servlet-api:3.0.1'
}